Simple example showing how to call the library and run the app.

Build and run example app:

```bash
go run ./cmd/app
```

Use the library in code:

```go
import "github.com/giova/template-go/pkg/greetings"

fmt.Println(greetings.Hello("Giova"))
```
